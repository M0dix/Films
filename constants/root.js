const ROOT_CATALOG = document.getElementById("films");
const ROOT_HEADER = document.getElementById("header");
const ROOT_FILMOGRAPHY = document.getElementById("filmography");
const ROOT_ADDER = document.getElementById("film-adder");